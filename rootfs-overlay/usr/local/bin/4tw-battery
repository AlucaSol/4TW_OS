#!/usr/bin/python3 -I
import sys
sys.path.insert(0, "/usr/local/lib/4tw")
from appliance import battery_overlay_text, notify
if len(sys.argv) != 1:
    raise SystemExit(2)
notify(battery_overlay_text())
