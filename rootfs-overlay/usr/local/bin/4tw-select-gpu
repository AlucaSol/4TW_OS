#!/usr/bin/python3 -I
import sys
sys.path.insert(0, "/usr/local/lib/4tw")
from appliance import preferred_drm_device
if len(sys.argv) != 1:
    raise SystemExit(2)
device = preferred_drm_device()
if device:
    print(device)
